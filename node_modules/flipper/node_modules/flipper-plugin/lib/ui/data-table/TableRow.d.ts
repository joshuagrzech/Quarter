import React, { CSSProperties } from 'react';
import type { TableRowRenderContext } from './DataTable';
export declare const DEFAULT_ROW_HEIGHT = 24;
declare type TableRowProps<T> = {
    config: TableRowRenderContext<any>;
    highlighted: boolean;
    record: T;
    itemIndex: number;
    style?: CSSProperties;
};
export declare const TableRow: React.MemoExoticComponent<(<T>({ record, itemIndex, highlighted, config, }: TableRowProps<T>) => JSX.Element)>;
export {};
//# sourceMappingURL=TableRow.d.ts.map
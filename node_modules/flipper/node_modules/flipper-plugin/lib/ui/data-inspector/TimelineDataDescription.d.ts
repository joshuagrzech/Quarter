import { Component, ReactNode } from 'react';
declare type TimePoint = {
    moment: number;
    display: string;
    color: string;
    key: string;
    properties: {
        [key: string]: string;
    };
};
declare type Timeline = {
    time: TimePoint[];
    current: string;
};
declare type Props = {
    canSetCurrent?: boolean;
    timeline: Timeline;
    onClick: (selected: string) => void;
};
declare type State = {
    selected: string;
};
export declare class TimelineDataDescription extends Component<Props, State> {
    constructor(props: Props);
    render(): ReactNode;
}
export {};
//# sourceMappingURL=TimelineDataDescription.d.ts.map
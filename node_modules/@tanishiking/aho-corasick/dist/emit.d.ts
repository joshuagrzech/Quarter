import { Interval } from './interval/interval';
export declare class Emit extends Interval {
    readonly start: number;
    readonly end: number;
    readonly keyword: string;
    constructor(start: number, end: number, keyword: string);
}
